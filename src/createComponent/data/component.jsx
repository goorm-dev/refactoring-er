import React from 'react';
import PropTypes from 'prop-types';

import style from './${name}.scss';

const ${name} = () => <div className={style.${name}}></div>;

${name}.propTypes = {};

${name}.defaultProps = {};

export default ${name};